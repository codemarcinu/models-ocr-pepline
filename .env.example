# Token Hugging Face (wymagany do diaryzacji pyannote/speaker-diarization-3.1)
HF_TOKEN=hf_your_token_here

# Konfiguracja Ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=SpeakLeash/bielik-11b-v2.3-instruct:Q5_K_M
OLLAMA_MODEL_FAST=llama3.2:latest
EMBEDDING_MODEL=mxbai-embed-large

# Konfiguracja Google Gemini (Hybrid AI)
GEMINI_API_KEY=your_google_ai_studio_key_here
GEMINI_MODEL=gemini-2.0-flash-lite

# Bezpieczeństwo i Logowanie
STRICT_MODE=True
LOG_LEVEL=INFO

# Baza danych Paragonów (opcjonalnie)
# RECEIPT_DB_URL=postgresql://user:password@localhost:5432/receipts_db
# RECEIPT_DB_URL=sqlite:////path/to/external/receipts.db

# API Security
API_SECRET_KEY=zmien_mnie_na_dlugi_losowy_ciag_znakow

# BrainGuard Threads
BRAIN_GUARD_MAX_WORKERS=3

# Phase 1: Async Receipt Pipeline (3-5x faster processing)
# Set to 'true' to enable async pipeline with smart caching
# Expected: 2-3s per receipt (vs 15-20s sync)
ASYNC_PIPELINE=false

# Phase 2: Bulk Database Operations (7x faster DB writes)
# Set to 'true' to enable bulk inserts/lookups for receipt sync
# Expected: 0.3s per receipt DB ops (vs 2.1s sync)
BULK_DB_OPS=false